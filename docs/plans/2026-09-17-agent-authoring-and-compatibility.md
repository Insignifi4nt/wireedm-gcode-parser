# Agent authoring and compatibility delivery

Requested by Cristian on 2026-09-17. Work on a PR branch; do not merge or deploy without review.

## Ordered scope

1. Review the working Robofil 2.5.0 post, exercise its capability/output fixtures, and publish a separately versioned package if its content changes. Preserve controller output unless evidence supports a correction.
2. Record breaking-change warnings in AGENTS.md. Establish app release tracking: remote main at 45f142c has 685 reachable commits, so the historical baseline is 0.0.685; this PR is 0.0.686. Future PRs increment the patch once, independently of commit count.
3. Add explicit post authoring app provenance, a release compatibility ledger/checklist, and PR verification. Unknown compatibility must not be presented as tested or inferred merely from a version number.
4. Give export errors a copyable agent repair prompt with app/post identity, exact diagnostics, known contract changes, source/documentation pointers, required evidence, and hosted verification actions. Preserve a manual copy fallback; do not silently include geometry or personal files.
5. Publish a concise /documentation/ entry point with static HTML, Markdown equivalents, schemas, SDK, fixtures, llms.txt discovery, and a sitemap. Document the whole journey from an empty agent context to a validated complete .wireedm-package. Correct existing contract drift. Do not claim WebMCP is implemented.
6. Run focused regressions, generated-doc parity, package conformance, build and full tests as appropriate. Open a PR with checkpoint commits, evidence, compatibility warning, and the new package location.

## Deferred work explicitly requested

- Repository hygiene: separate personal machine/postprocessor artifacts from distributable examples and remove obsolete files after auditing test/evidence dependencies. Do not delete them as part of this work.
- Persistence audit: map browser-cache and folder layouts, identify orphaned legacy files, design safe cleanup and a versioned migration mechanism with backups, recovery, interrupted-upgrade handling, and tests. Preserve browser-cache-only and optional-folder workflows.
- After WebMCP implementation: document the actual shipped tools, permissions, discovery, inputs/outputs, and agent workflows, with examples verified against the implementation.
- Dependency maintenance discovered during this work: npm audit reports existing Vite, PostCSS and nanoid advisories (plus development dependencies). Review/update separately; this PR adds only the build-time Markdown renderer. Remove the now-unused gh-pages dependency when cleaning the old deployment tooling.

## Completion record

- PR: https://github.com/Insignifi4nt/wireedm-gcode-parser/pull/1 (review before merge; no deployment performed).
- Robofil 2.6.0 adds app provenance. Regression verifies the exact published 2.5.0 contract hash after removing only the new metadata and version; old and new releases both pass installation conformance. The built archive is `examples/robofil-100-v2/cristian-robofil-100-v2.wireedm-package`, SHA-256 `48bcc0bdf14c197495fd0933b62173d22d598f4d71ae2ca861d0dd2245acde62`. Keep using 2.5.0 on the current live app until 0.0.686 is deployed.
- Root agent rules, PR template/CI, app version display, release ledger and immutable source-tag workflow are implemented. Remote branch protection is unchanged; the repository owner can require the `Review checks / verify` status check.
- Export errors include human guidance, exact diagnostic context and a reviewable/copyable agent prompt with clipboard-denial fallback. No user project was changed.
- Static `/documentation/` pages, Markdown, published contracts, scoped `llms.txt`, sitemap and release records are generated by the normal build. The current entry point distinguishes app, post, UPID and guest API versions; stale schema-v1-only metadata is replaced with schema-derived versions. Browser preview and all 49 generated file links were checked.
- Verification: full suite 139 files / 1,358 tests; 38 focused tests; final export-context regression run 10 tests; typecheck/build; post documentation parity; release gate against origin/main; Robofil conformance and archive validation; whitespace check. All passed locally. GitHub CI result is attached to the PR.
- The three explicitly deferred requests above remain open; no broad repository cleanup, storage migration or WebMCP implementation is bundled into this release.

## Browser-only authoring correction

- Public guides, published release notes, task checklist, agent contract instructions and copied repair prompts now assume only a cloud browser. Removed required repository/CLI workflows and moved deployment/discovery rationale and maintainer release procedures into `docs/maintainers/documentation-and-releases.md`.
- Added `/package-tools/`: paste/upload and check post JSON with canonical hashes and actual conformance output; hash evidence; build complete packages; inspect packages and reuse safely decoded documents/evidence for repairs. Failed validation cannot offer an installable archive. Inputs are ephemeral and do not access the workbench's storage. Expensive checks run in a cancellable worker.
- Shared storage-free domain operations support a future WebMCP adapter. No WebMCP implementation or discovery claim is introduced.
- Browser preview verified QuickJS conformance on the production worker, package inspection, reuse and byte-identical rebuild (the existing archive SHA-256 above), and a missing-evidence diagnostic after changing an evidence path. Input edits clear old results. The browser automation download event timed out; a UI regression independently verified the exact Blob bytes, filename and download anchor, plus cancellation and stale-result removal.
- Final verification: 141 test files / 1,364 tests passed; build/typecheck; 14 static documentation pages and 51 published files/link checks; generated contract parity; release check against origin/main; whitespace check. This remains the same unmerged 0.0.686 PR.
